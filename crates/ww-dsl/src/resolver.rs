// Name resolution is integrated directly into the compiler (compiler.rs).
// This module is reserved for future standalone resolution passes
// (e.g., cross-file resolution, incremental re-resolution for LSP).
